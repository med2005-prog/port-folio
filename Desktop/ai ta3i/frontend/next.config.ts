import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // N-forcew l-server y-sme3 l-kolchi
  // (Optional, l-command line flag kafi 3adatan, labass n-zidouh)
  reactStrictMode: true,
};

export default nextConfig;
